# Feature: MultiPayments — módulo VIP, canales de pago y multi-pago por venta

**Creado**: 2026-09-18 · **Estado**: CERRADA (T1–T14 completas y revisadas; ver Progreso y Próximo paso) (T1–T6 + T13 cerradas; **review slice 2 APPROVED + acknowledged** vía successor `review-7d3f9a1c2e5b8046` — authority burned; advisories del review resueltos (`1dceb16d`, `29aa843a`, `3b364c55`); T5 cerrada (`91802533`); T6 cerrada (`de0aa66f`); **slice post-T6 APPROVED + acknowledged** (lineage `review-4a8ee3ac9f7fced5`, 4R, authority burned); advisories resueltos (`53fa4f10`) y review del fix **APPROVED + acknowledged** (`review-24925483e75e237b`); reporte upstream del defecto diferido) · **Rama**: `feat/multipayments` (feature-branch-chain, base `4b8a54dc`; origin default = `main`)
**Fuente funcional**: `docs/contracts/pagos-y-canales-de-pago.md` (RF-01…RF-12), adaptada por las decisiones del owner (2026-09-18).
**Referencia de implementación**: MultiMonedas (módulo 15) → migración `20260917143901_Add-MultiMonedas-Module`, `MultiMonedasModuleBackfill.cs`, script `backend/scripts/17-20260917-Add-MultiMonedas-Module.sql`.

## Objetivo

Habilitar en el POS (React) el módulo **MultiPayments** para tiendas VIP: varias formas de pago en una venta con conversión por tasa, canales de pago generalizados (método + moneda sobre las 7 monedas), vuelto al pagar de más, selector de moneda del carrito y registro de tasas por canal (append-only), con el mínimo de cambios posible sobre la arquitectura actual (POS offline-first + espejo backend).

## Problema

Hoy una venta tiene UNA forma de pago y UNA moneda (el carrito bloquea mezclar monedas: `currency-guard`). El contrato define el modelo nuevo (0..N pagos por orden, tasas congeladas, cascada de resolución, un solo redondeo HALF-UP) y **no está implementado**: no existen `OrderPayment`, canales, tasas por canal ni conversión entre monedas.

## Por qué

Pedido del owner (2026-09-18) tras analizar `docs/contracts/pagos-y-canales-de-pago.md`. 8 decisiones confirmadas por el owner antes de planificar. El módulo se suma al plan VIP, siguiendo el patrón ya probado de MultiStores/MultiMonedas (migración EF + script VPS con backfill).

## Alcance

**Dentro**:

- Módulo `MultiPayments` (id 16) + feature 44; `Price=10`, `PercentDiscountPrice=50` (efectivo 5); **solo VIP (4)**; backfill de tiendas VIP activas.
- Canales generalizados: canal = método + moneda sobre las 7 monedas (CUP, USD, EUR, CLA, MLC, CAD, MXN); los 5 canales del doc son un subconjunto.
- Varios pagos por venta: canal, monto en su moneda, tasa congelada, monto convertido a la moneda de la orden; vuelto al pagar de más.
- Registro de tasas por canal append-only (valor moneda-por-USD, vigencia, 6 decimales) + UI dueño/admin + persistencia offline + sync.
- Selector de moneda del carrito (solo con módulo 16) con persistencia por usuario.
- Multi-moneda en carrito habilitado por módulo 16 (el guard actual se mantiene sin módulo).
- Espejo backend de paridad: `OrderPayment` + `ChannelExchangeRate` (sin endpoints nuevos).
- Migración EF + script VPS `18-20260918-Add-MultiPayments-Module.sql`.

**Fuera**:

- Flujo de crédito y endpoints nuevos (RF-12) — el POS sigue offline-first.
- percent/tax con multi-pago (con módulo, el total es la suma de líneas convertidas).
- Conversión automática desde el registro diario USD→CUP existente (no se toca).
- Tocar E2E existentes, Angular legacy, o producción backend fuera de lo listado.

## Reglas vigentes (no negociables)

- E2E backend (`backend/src/SMCA.WebApi.E2ETests/`) y frontend (`frontend-react/e2e/` + `support/`): **prohibido modificar, borrar, renombrar, skipear o debilitar existentes**. Solo tests NUEVOS (archivos nuevos).
- Backend production: requiere notificación + aprobación explícita del owner antes de tocar (aplica a T1/T2 — notificado y **autorizado** 2026-09-18).
- Angular (`frontend/`) frozen; todo en `frontend-react/`.
- Código y comentarios en inglés; UI en español (`app/shared/lib/i18n/es.ts`).
- Commits convencionales, sin atribución AI; cada tarea cierra con commit work-unit + evidencia observada.

## Decisiones ratificadas por el owner (2026-09-18)

1. **Canales generalizados**: canal = método + moneda (las 7 monedas). Sin tasa resoluble, el pago solo se acepta en su propia moneda.
2. El **CUP del doc = CUP de la app**.
3. **Arquitectura**: frontend-first + **espejo backend de paridad** (tablas de pagos y tasas; sin endpoints). Migración EF + script VPS.
4. **Tasas por canal**: registro append-only (canal, valor moneda-por-USD, vigencia, 6 decimales, UI propia). Cascada: tasa del canal → tasa de la misma moneda (otro canal) → pivote USD=1 → error tipado. Congelada por pago.
5. **Vuelto**: se puede pagar de más; el sobrante se muestra como vuelto; no se cierra por debajo del total; sin crédito.
6. **Módulo**: id 16 + feature 44, Price 10 / 50%, solo VIP, backfill de tiendas VIP activas (patrón script 17).
7. **Selector de moneda**: CUP, USD + las monedas del carrito; define la moneda del total; persistido por usuario en el dispositivo.
8. **percent/tax no aplican con multi-pago**; sin módulo 16, el comportamiento actual queda intacto.

## Decisiones finas (propuestas, revisables)

- **D-f1 — Vuelto**: los pagos persistidos son los **aplicados** (Σ = total exacto, invariante RF-04); el sobrante se muestra como vuelto en la moneda del pago que excede.
- **D-f2 — Espejo backend**: cubre `OrderPayment` **y** `ChannelExchangeRate` (paridad §2.3 del doc).
- **D-f3 — UI de tasas**: se decide en T5 cuidando no romper los E2E existentes de `management/exchange-rates` (sección nueva en la vista actual o ruta nueva).
- **D-f4 — TDD**: `disabled` según el registro de testing (el `strict_tdd: true` de `openspec/config.yaml` pertenece a un pipeline retirado). Checks funcionales + tests nuevos en cada tarea. Runners: `pnpm test` / `dotnet test`.
- **D-f5 — Naming**: `ChannelRate` (frontend) / `ChannelExchangeRate` (backend) para no colisionar con el `ExchangeRate` diario existente del frontend.
- **D-f6 — Escala decimal backend**: `ApplicationDbContext.OnModelCreating` fuerza `18,6` a todo decimal; se añade un guard que respeta tipos explícitos (comportamiento equivalente para el resto) y los montos espejo quedan `18,2` mientras las tasas siguen `18,6` (doc §2.3).

## Tasks

- [x] **T1 — Backend: módulo + catálogo + espejo + migración EF.** `ModuleType.MultiPayments=16`, `FeatureType.MultiPayments=44`; HasData de `Module` (Price 10 / Percent 50), `Feature` (→ módulo 16) y `StorePlanModule` (solo VIP); entidades `OrderPayment` y `ChannelExchangeRate` + configuraciones EF; migración. **AC**: `dotnet build` verde; migración aplicable en `smca_test`; sin cambios en tests existentes, salvo la **excepción autorizada 1:1 (2026-09-18)**: `StorePlanCatalogTests.cs` (matriz Superior 2..15 / VIP 2..16). **Cerrada 2026-09-18**: build 0 errores; E2E 1/1; migración `20260918131144_Add-MultiPayments-Module-And-Payment-Mirror` aplicada a `smca_test`; sin drift de modelo.
- [x] **T2 — Script VPS 18.** `backend/scripts/18-20260918-Add-MultiPayments-Module.sql` espejo de la migración: catalog INSERTs, `StorePlanModule` (VIP), backfill `StoreModule` + `StoreRoleFeature` (roles 2/3) para tiendas VIP activas, setval, registro en `__EFMigrationsHistory`, SELECTs de verificación, ROLLBACK comentado. **AC**: paridad 1:1 con la migración; idempotente (`ON CONFLICT DO NOTHING`). **Cerrada 2026-09-18**: DDL 1:1 (solo guards de idempotencia); backfill idéntico a los constantes; smoke test psql 2 corridas contra `smca_test` → exit 0 (no-op); `backend/scripts/README.md` actualizado (fila 18).
- [x] **T3 — Frontend domain: canales + tasas + conversión.** Modelo de canal (método+moneda), resolución en cascada, conversión vía pivote USD con **un solo redondeo HALF-UP**, modelo `OrderPayment`, helpers de vuelto. Tests unitarios nuevos. **AC**: casos §4.1 del doc cubiertos. **Cerrada 2026-09-18**: `ChannelRate`/`OrderPayment` + errores tipados (`channel-rate-errors`), `channel-conversion.ts` (cascada canal→moneda→pivote USD→error tipado; conversión en enteros con un único `divideHalfUp`), `payment-tally.ts` (aplicado/restante/vuelto); 25 tests nuevos (2 archivos); `pnpm typecheck` (domain) limpio; suite domain 156/156.
- [x] **T4 — Registro de tasas (offline).** Servicio append-only por canal (storage cifrado por tienda), integración en sync (`data-serializer`) y provisión. Tests. **AC**: alta + lectura de la tasa vigente por momento; filas nunca editadas ni borradas. **Cerrada 2026-09-18**: `ChannelRateOfflineService` (`management/channel-rates`) — append-only, ids UUID en alta y determinísticos en import, fechas revividas, sin update/delete; entidad `channelRates` (11ª) + `channel-rates.json` en export/import + merge append-only en `data-synchronizer` + rutas export/import; 28 tests nuevos (4 archivos). Evidencia: vitest dirigido 28/28 · 287/287; suite completa 263/263 · 3764/3764; `turbo typecheck` 5/5; `turbo lint` 4/4; commit `b20f63fc`; RDD assess **medium** (base `fbf97298`) → diferido.
- [x] **T5 — UI de tasas por canal.** Pantalla (gating dueño/admin, mismo criterio que la vista de tasas actual) para registrar tasa por canal con vigencia + historial. Tests. **AC**: registrar + ver historial; gating correcto; E2E existentes de la vista de tasas intactos. **Cerrada 2026-09-18**: ruta NUEVA `management/channel-rates` (D-f3: ruta nueva, no sección en la vista de tasas, para no tocar sus E2E) — `channel-rates.tsx` con `adminFeatureLoader([EFeatures.Configurations])`, formulario método+moneda+valor+vigencia (`registerRate`, error tipado visible), historial append-only (sin editar/borrar) ordenado; registro en `routes.ts` + ítem de menú sin icono + claves i18n `MENU.CHANNEL_RATES`/`CHANNEL_RATES.*`. Evidencia: test de ruta nuevo (gating, alta, error, ausencia de delete); vitest dirigido 75/75; suite completa 3774/3774; typecheck 5/5; lint 4/4; commit `91802533` (5 archivos, +501).
- [x] **T6 — Selector de moneda del carrito.** Solo con módulo 16; CUP, USD + monedas del carrito; define la moneda del total; persistencia por usuario (sobrevive recargas). Tests. **AC**: selección persistida y reutilizada en la próxima venta. **Cerrada 2026-09-18**: enums `EModules.MultiPayments=16`/`EFeatures.MultiPayments=44` + `hasMultiPaymentsModuleAvailable` + preferencia por usuario (`lizoft.cart-currency-${userId}`, `cart-currency-preference.ts`, default CUP, SSR-safe) + selector `multipayments/cart-currency-select.tsx` (solo con módulo 16; CUP, USD + monedas del carrito) usado por `CartShell` como moneda del total (sin conversión: eso es T8; `currency-guard` intacto); i18n `SHOPPING_CART.CURRENCY_LABEL`. Evidencia: vitest dirigido 60/60; suite completa 3785/3785; domain 164/164; typecheck 5/5; lint 4/4; commit `de0aa66f` (9 archivos, +365/−1).
- [ ] **T7 — UI multi-pago en carrito.** Lista de pagos (agregar/quitar), cada uno canal+moneda+monto; conversión a la moneda de la orden; restante por cubrir; vuelto al exceder; bloqueo de cierre por debajo del total. Tests. **AC**: escenarios del owner (USD 120 = 100 USD + 14.000 CUP @700; CUP 120 = 100 CUP + 20 CUP).
- [x] **T8 — Multi-moneda en carrito (módulo 16).** Levantar `currency-guard` cuando el módulo está activo; líneas y total convertidos a la moneda elegida. Tests. **AC**: con módulo, carrito mixto permitido; sin módulo, guard intacto. **Cerrada 2026-09-19**: `guardCurrency` gana `allowMixedCurrencies?: boolean` (default false: sin módulo, comportamiento byte-idéntico); los 4 call sites (`sale.tsx` ×2, `wholesale.tsx`, `egress.tsx`) pasan `hasMultiPaymentsModuleAvailable(user)`. Nuevo helper puro `multipayments/cart-line-conversion.ts` (convierte el precio unitario de cada línea vía `convertLineAmount` — RF-09 — y suma el total; cross-currency sin tasa → error tipado, nunca 0). `cart-shell` usa el total convertido como total de venta cuando el multi-pago está activo, pinta cada línea con su equivalente, unifica `multiPaymentOrderCurrency = saleCurrency` (reconcilia la tensión T6) y bloquea el submit si una línea no convierte; al registrar, pasa los ítems con precio convertido y `product.currency = saleCurrency` (clon shallow, sin mutar el store) para que la orden persista en la moneda de venta. Evidencia: dirigido 76/76; suite app **3829/3829**; `turbo typecheck` 5/5; `turbo lint` 4/4; commit `21691443`.
- [ ] **T9 — Orden: persistencia.** `createOrder`/`reviveOrder` con `payments[]`; compatibilidad con campos legacy (`salePaymentMethod`, `percent`, `tax`); backfill de órdenes viejas. Tests. **AC**: round-trip y compatibilidad sin regresión.
- [x] **T10 — E2E frontend (spec NUEVO).** `frontend-react/e2e/multipayments.spec.ts`: selector de moneda, multi-pago, vuelto, tasas. **AC**: verde contra app+backend reales; ningún spec existente tocado. **Cerrada 2026-09-19**: spec NUEVO + fixture NUEVO (`support/multipayments-fixture.ts`); **2/2 VERDE** contra app+backend reales (32.5 s, sin retries). Cubre: gate del módulo 16 (precondición "tienda nueva no tiene 16", ya verdadera tras T14), selector de moneda (CUP/USD + persistencia por usuario), línea CUP sin tasa → error tipado `RateNotFound` que bloquea la venta, tasa CUP registrada → conversión, multi-pago (USD+CUP cubren el total), venta registrada, vuelto por sobrepago y bloqueo por subpago. **Root cause de la primera corrida roja** (más allá de la precondición): el login real deja el DEK en memoria → las entidades `lizoft.store-*` se escriben **cifradas** (`enc:v1:`), y el seeder directo a `localStorage` hacía `JSON.parse` → throw → `return` sin stock (diálogo "El producto no está disponible en el inventario"). Fix: `dropDekAndCiphertext` (espeja EXACTAMENTE la exclusión de `support/session.ts`: `lizoft.device-dek` + `lizoft.store-*` con valor `enc:v1:`, + `currentUser`) y seeder honesto que falla ruidoso en vez de no-op silencioso. Commit `a7f656ba`. **Review RDD: APPROVED + acknowledged (`authority: burned`)** — lineage `review-f6f93cecbf81453e`, tier medium, 1 lente (`review-reliability`), 2 paths / 523 líneas. **3 advisories NO bloqueantes**: `R3-shared-serial-state` (WARNING — T10.2 consume `identity`/`storeId` módulo-scoped que solo puebla T10.1; si se corre aislado falla opaco en vez de con un error de orden), `R3-exact-option-order` (SUGGESTION — `toEqual(['CUP','USD'])` pinea el set y orden exactos), `R3-class-locator` (SUGGESTION — el total convertido se aserta con un locator de clase CSS, no un testid).
- [x] **T11 — E2E backend (archivo NUEVO).** Persistencia espejo de `OrderPayment`/`ChannelExchangeRate` + defaults. Add-only. **AC**: verde en `smca_test`. **Cerrada 2026-09-19**: archivo NUEVO `backend/src/SMCA.WebApi.E2ETests/Stores/MultiPaymentsPersistenceMirrorTests.cs` (320 líneas, add-only; ningún E2E/source existente tocado). `MPM1` valida los tipos físicos reales vía `information_schema` (`OrderPayment.Amount`/`AmountInOrderCurrency` = `numeric(18,2)`, `RateApplied` = `numeric(18,6)`, `ChannelExchangeRate.Value` = `numeric(18,6)`) + el modelo EF; `MPM2` round-trip de `OrderPayment` con las dos procedencias de tasa (pivote USD sintético con `RateMethod/RateCurrency/RateEffectiveFrom` NULL, y tasa de canal congelada con los 3 seteados); `MPM3` append-only: dos filas para el MISMO canal (Method, Currency) coexisten sin sobrescritura, `Value` a 6 decimales. Evidencia: `dotnet build -c Release` 0 errores; filtro `MultiPaymentsPersistenceMirrorTests` **3/3 verde** (spot-check del orchestrator re-ejecutado: 3/3). Commit `91144be3`. **Review RDD: APPROVED + acknowledged (`authority: burned`)** — lineage `review-4c6eae256896cccb`, tier medium, 1 lente (`review-reliability`), 1 path / 320 líneas. **2 advisories NO bloqueantes** (SUGGESTION): `R3-seed-outside-try` (el `SeedOwnerAsync` corre fuera del `try`; un fallo a mitad deja filas huérfanas en `smca_test`) y `R3-cleanup-unguarded` (los dos cleanups del `finally` no están guardados individualmente).
- [x] **T12 — Cierre.** Verificación completa (vitest/typecheck/lint + dotnet build/tests + E2E nuevos), actualizar este doc con evidencia, commits work-unit. **Cerrada 2026-09-19**: `pnpm turbo run typecheck` 5/5; `pnpm turbo run lint` 4/4; `pnpm turbo run test` **4004/4004** (app 3829, domain 164, web-common 11; 287 archivos); `dotnet build backend/src/SMCA.sln -c Release` **0 errores**; `dotnet test backend/src/SMCA.sln -c Release` **1049/1052** (Domain.UnitTests 27/27, Application.Tests 487/487, E2E 535/538) con los **3 fallos PREEXISTENTES** por el módulo 17 out-of-tree en `smca_test` (nada más falla). E2E nuevos: frontend `multipayments.spec.ts` 2/2 y backend `MultiPaymentsPersistenceMirrorTests` 3/3 (ya verificados en T10/T11).
- [x] **T14 — Backend: el alta otorga los módulos del PLAN por defecto (gating VIP real del 16).** `RegisterCommand` debe resolver el plan por defecto (Superior, 3) con sus `StorePlanModules` y pasar esos ids a `CreateStoreService`, en vez de `GetAvailableModulesToStore()` (todos los `AvailableToStore`). Así una tienda nueva NO nace con el módulo 16 (VIP-only; Superior = 2..15). El `AvailableToStore` del módulo 16 y de la feature 44 se mantiene `true` (permite adicionarlo después a una tienda). **Autorización owner 2026-09-19**: tocar producción backend + modificar el E2E existente `AuthRegisterDataAssertionsTests.Register_assigns_all_available_modules_including_paid` (que hoy pinea la igualdad exacta contra TODOS los `AvailableToStore`). **AC**: alta nueva → `StoreModule` = plan Superior (sin 16); unit tests de Register actualizados; E2E backend verde; sin cambios de catálogo/migración/script. **Bloquea a T10** (su precondición "la tienda nueva no tiene 16" pasa a ser verdadera). **Cerrada 2026-09-19**: `RegisterCommand` resuelve `GetActivePlanWithModulesByIdAsync(Superior)` y otorga sus `StorePlanModules` (null/exception → `Register.PlanLoadFailed`); se quitó `IModuleRepository` del handler; unit tests de Register reescritos al modelo de plan; E2E `AuthRegisterDataAssertions.Register_assigns_default_plan_modules` + `NotContain(16)`. Evidencia: `dotnet build -c Release` 0 errores; `Application.Tests --filter Register` 108/108; E2E `--filter AuthRegister` 21/21 (spot-check del orchestrator re-ejecutado: 21/21); suite E2E completa **532/535 con 3 fallos PREEXISTENTES** (módulo 17 out-of-tree en `smca_test`; fallan idéntico con/sin el cambio, verificado por stash). Commit `2dac7dbb`. **Review RDD: APPROVED + acknowledged (`authority: burned`)** — lineage `review-30a374906c71497f`, 4R, riesgo high (hot path auth), 5 paths / 264 líneas.
- [x] **T13 — Follow-up del review (secuencias EF).** Advisory R3-SequenceDivergence: la migración `20260918131144` no incluye los `setval` de `Feature`/`Module` que sí tiene el script 18 → en DBs frescas aplicadas por `dotnet ef database update` la secuencia puede quedar detrás de los ids sembrados (16/44). Fix pequeño (setval en la migración o migración follow-up) + nota de paridad. No bloqueante. **Cerrada 2026-09-18**: `MultiPaymentsModuleBackfill.SequenceFixupsSql` + llamada en la migración; build 0 errores; commit `c6fae90d`.

## Verificación por tarea

- Frontend: `npx vitest run <paths>`, `pnpm turbo run typecheck`, `pnpm turbo run lint`.
- Backend: `dotnet build backend/src/SMCA.sln`; tests nuevos por filtro; E2E nuevos con PostgreSQL `smca_test`.
- Cada tarea cierra con evidencia observada anotada en este doc + commit work-unit.

## Delivery

- Forecast (authored, sin generados): **~3.5k–4.5k líneas** → supera el presupuesto de revisión (~400) ⇒ estrategia `ask-on-risk` (default): la cadena (`stacked-to-main` vs `feature-branch-chain`) se define antes del primer commit.
- Rama: hoy `qa` (no es la default de origin); si el owner prefiere rama feature, se crea antes del primer commit.

## Progreso

- 2026-09-18: feature doc creado; 0/12 tareas. Limpieza: `openspec/changes/multipayments/` (carpeta vacía de un enrolamiento SDD erróneo) eliminada.
- 2026-09-18: primer bloqueo E2E resuelto — el owner autorizó 1:1 la actualización de `StorePlanCatalogTests.cs` (VIP 2..16); ningún otro test afectado (verificado).
- 2026-09-18: **T1 cerrada** — rama `feat/multipayments` (cadena `feature-branch-chain`); migración `20260918131144_Add-MultiPayments-Module-And-Payment-Mirror`; evidencia: `dotnet build` 0 errores + E2E `StorePlanCatalogTests` 1/1 + `has-pending-model-changes` sin drift.
- 2026-09-18: fix de paridad de T1 — `MultiPaymentsModuleBackfill.cs` (backfill `StoreModule`/`StoreRoleFeature` para VIP activas, `ON CONFLICT DO NOTHING`) + llamadas en Up/Down de la migración; build 0 errores.
- 2026-09-18: RDD — assessment del commit `89c0a24d`: **medium → diferido al slice** (boundary `4b8a54dc`).
- 2026-09-18: **T2 cerrada** — script `18-20260918-Add-MultiPayments-Module.sql` + `backend/scripts/README.md`; DDL/backfill 1:1 con la migración; idempotencia verificada con psql (2 corridas, exit 0); RDD del slice (2 commits): **medium → diferido al slice**.
- 2026-09-18: **Review RDD del slice 1: APPROVED + acknowledged (authority burned)** — el lente `review-reliability` corrió tras el reinicio con `opencode-go/deepseek-v4.1-flash` + `variant: low` (workaround del gate free-tier de OpenCode; los intentos previos con big-pickle dieron 403 del gate — incidente upstream #49433). Hallazgos advisory (NO bloqueantes): **R3-MissingCoverage** (persistencia/backfill sin aserción → lo cubre T11) y **R3-SequenceDivergence** (la migración EF no lleva los `setval` de secuencias que sí tiene el script 18 → nuevo T13). Boundary revisado: commit `5f08cdee`.
- 2026-09-18: **T13 cerrada** — `MultiPaymentsModuleBackfill.SequenceFixupsSql` + llamada en la migración (paridad con script 18); build 0 errores. Commit `c6fae90d` (RDD assess: medium → diferido al slice 2).
- 2026-09-18: **T3 cerrada** — domain: `ChannelRate`/`OrderPayment`/errores tipados + `channel-conversion` (cascada + pivote USD, un solo HALF-UP con enteros) + `payment-tally` (aplicado/restante/vuelto); 25 tests nuevos verdes; typecheck limpio; suite domain 156/156. (Primer intento del subagente murió por el trap conocido de variant=max — 32k reasoning, output vacío; el reintento con pasos pequeños salió verde.)
- 2026-09-18: **Review RDD del slice 2 (T13 + T3), lineage `review-111b2dd61cb19792`**: lente reliability admitido; el refuter **corroboró 1 hallazgo CRITICAL** — `R3-SameCurrencyRatio` (conversión misma-moneda podía reescalar montos: 555 EUR → 666 EUR si una tasa más nueva de otro método existe para la misma moneda). **Corrección acotada aplicada** (24 líneas declaradas): `convertPaymentAmount` aplica LA MISMA tasa resuelta en ambos lados (identidad exacta) + test de regresión multi-canal; `pnpm typecheck` limpio; suite domain **157/157**; commit `fbf97298` (transacción committed-only ⇒ el fix debía commitearse; solo los 2 archivos, AGENTS.md/CLAUDE.md staged intactos).
- 2026-09-18: **⚠️ Review slice 2 BLOQUEADO tras el commit** — el STATUS bound (única re-entrada del review) falla con `operation_failed` / `validate negotiated review status: negotiated STATUS repository context is invalid` (retry_safe, `mutation_outcome: not_started`; gentle-ai **v3.1.0**, el propio código marca el mismatch como "requires a code fix"). Antes del commit el mismo comando devolvía el stop esperado `corrected_candidate_unavailable`. Hay releases nuevas de hoy (v3.2.1, v3.3.0) pero los diffs revisados no muestran fix directo de este path. Estado del store en `.git/gentle-ai/review-transactions/v2/review-111b2dd61cb19792/review-state.json` (revision `45abe389…`, capture_phase_revision `b1245920…`).
- 2026-09-18: hallazgos advisory restantes del lente (NO bloqueantes, para T11/T12 o follow-ups): `R3-SilentSkipNonPositive` y `R3-OrderDependentLatest` (WARNING); `R3-NegativeRoundingContract` (comentario de contrato de `divideHalfUp` para negativos) y `R3-SequenceFixupUnproved` (setval sin test que lo asevere) (SUGGESTION).
- 2026-09-18: reintento del STATUS bound con **gentle-ai v3.3.0** (el binario local ya está actualizado): **mismo defecto** (`operation_failed` / `negotiated STATUS repository context is invalid`; retry_safe, not_started, sin mutación). Binding verificado (base `5f08cdee` = tree `d50d7432…`, `--committed-only`, `--lineage`). No se tocó nada más del review ni del store.
- 2026-09-18: owner eligió **reportar + continuar**; el reporte upstream quedó **diferido** (no hay `gh` en PATH y el owner pidió continuar sin desvíos). Review slice 2: estado preservado, sin más intentos. **T4 cerrada** — commit `b20f63fc` (17 archivos, +1058/−4; 28 tests nuevos): vitest dirigido 287/287 y suite completa 3764/3764; typecheck 5/5; lint 4/4; RDD assess **medium** (base `fbf97298`) → diferido. Gotcha: domain con `dist` stale → `pnpm --filter @store-mgmt/domain build` antes de vitest en `web-store-pos`.
- 2026-09-18: **Review slice 2 DESBLOQUEADO y CERRADO** — el STATUS bound dejó de fallar (el candidato movido pasó a `scope_changed`); con autorización del owner se ejecutó `gentle-ai review recover` (binding LF `predecessor_lineage/predecessor_revision/target_identity/actor/reason`; successor `review-7d3f9a1c2e5b8046`) y el lente `review-reliability` fue **ADMITIDO → APPROVED**; `acknowledge-approved` → `authority: burned` (consumed revision `342b1234…`). Candidato revisado: base `5f08cdee` committed-only → **slice 2 + T4**. Predecessor `review-111b2dd61cb19792` preservado.
- 2026-09-18: advisories del review cerrado (NO bloqueantes, informativos): `R3-ImportValueUnvalidated` (WARNING — `addImportedChannelRate` no valida `value`; un 0/NaN importado puede hacer que `divideHalfUp` lance en vez de devolver error tipado), `R3-WriteWhileLockedThrows` (WARNING — `registerRate` con DEK bloqueado lanza en vez de devolver `DataResult` fallido; sin test), `R3-DerivedIdDropsDistinctRows` (SUGGESTION — el id derivado ignora `value`: dos filas distintas con mismo canal+momento colapsan en import), `R3-SequenceFixupUnverified` (SUGGESTION — setval sin aserción).
- 2026-09-18: **advisories RESUELTOS antes de T5** (pedido del owner) — `1dceb16d` (`fix(review): validate imported channel rates and keep derived ids value-unique`; 7 archivos): **F1** el import seam rechaza `value` no finito/≤0 con `Result` fallido (`ChannelRateOfflineErrors.InvalidValue`) y el cascade domain ignora filas no-usables (`isUsableRate`) → nunca throw/NaN, cae al `RateNotFound` tipado; **F2** el throw de `MissingDataKeyError` al escribir bloqueado es **por diseño** (igual que `ExchangeRateOfflineService`); se **fija con test** sin cambiar comportamiento; **F3** el id derivado incluye `value` en servicio y synchronizer (re-import idempotente, sin pérdida silenciosa). `29aa843a` (`test(backend)`; 1 archivo, 2 tests): **F4** wiring determinístico (la migración registra el `SequenceFixupsSql`) + efecto real (`nextval > MAX("Id")` para Module/Feature contra PostgreSQL). Evidencia: vitest dirigido 29/29 y domain 158/158; `turbo typecheck` 5/5; `turbo lint` 4/4; `dotnet build` 0 errores; `dotnet test --filter MultiPaymentsSequenceFixupTests` 2/2. RDD assess (base `b20f63fc`): **medium** (8 archivos, 261 líneas) → diferido al slice.
- 2026-09-18: **3 advisories del lineage anterior RESUELTOS** — `3b364c55` (`fix(domain): reject invalid tally input, make rate ties deterministic, and honor half-up signs`; 4 archivos): **A1** `R3-SilentSkipNonPositive` — `summarizePayments` ya no ignora montos ≤0 en silencio; delega en `applyPayment` (error tipado `PaymentTally.NonPositiveAmount`); **A2** `R3-OrderDependentLatest` — el empate por `effectiveFrom` se resuelve con un orden total determinístico (`effectiveFrom` → `createdDate` → `id`), independiente del orden del array; **A3** `R3-NegativeRoundingContract` — `divideHalfUp` ahora cumple su contrato half-away-from-zero para todo signo. Evidencia: domain 161/161; web-store-pos (channel-rates+sync) 156/156; typecheck 5/5; lint 4/4. RDD assess (base `b20f63fc`): **medium** (10 archivos, 369 líneas) → diferido.
- 2026-09-18: **T5 cerrada** — `91802533` (`feat(channel-rates): add gated owner/admin UI to register channel rates`; 5 archivos, +501): ruta nueva `management/channel-rates` (decisión D-f3: **ruta nueva**, no sección en la vista de tasas, para no tocar sus E2E), gating `adminFeatureLoader([EFeatures.Configurations])` (mismo criterio que la vista de tasas), alta de tasa por canal con vigencia + historial append-only (sin editar/borrar); tests de ruta (gating/alta/error/sin delete). Evidencia: vitest dirigido 75/75; suite completa 264 archivos · 3774/3774; typecheck 5/5; lint 4/4. RDD assess (base `b20f63fc`): **medium** (15 archivos, 870 líneas) → diferido al slice.
- 2026-09-18: **T6 cerrada** — `de0aa66f` (`feat(cart): add module-gated cart currency selector with per-user persistence`; 9 archivos, +365/−1): enums 16/44, `hasMultiPaymentsModuleAvailable`, preferencia por usuario, selector gated por módulo 16 (CUP, USD + monedas del carrito), `CartShell` usa la preferida como moneda del total (conversión = T8). Evidencia: vitest dirigido 60/60; suite completa 3785/3785; domain 164/164; typecheck 5/5; lint 4/4. **RDD assess del slice: `high`** (`hot_path` `authorization-service.ts`; 23 archivos, 1236 líneas) → START preflightado; **consentimiento pendiente** (`review-4a8ee3ac9f7fced5`).
- 2026-09-18: **Review del slice acumulado (F1–F4 + A1–A3 + T5 + T6) CERRADO — APPROVED + acknowledged (`authority: burned`)** — lineage `review-4a8ee3ac9f7fced5`, 4 lentes (risk, resilience, readability, reliability), base-tree `c04377da…` (= `b20f63fc`), 23 archivos / 1236 líneas. **11 advisories, todos NO bloqueantes** (informativo): WARNING — `R1/R3/R4-PaymentTallyThrow` (el tally ahora **lanza** con una línea ≤0 en vez de ignorarla; ningún call site lo cubre aún), `R1-ChannelRateIdScheme` (el id derivado pasó a incluir `value`: filas id-less persistidas con el esquema viejo se re-importan como duplicados), `R3-CartCurrencyFallback` (el selector muestra CUP si la preferida no está entre las opciones, sin corregir al padre → etiqueta vs total pueden discrepar), `R2/R3-DuplicateModuleGate` (gate de módulo 16 duplicado: helper local vs `authorization-service`); SUGGESTION — `R2-HardcodedCurrencyLabels` (CUP/USD hardcodeados vs `currencyLabel`), `R2-ErrorNameDoc` (`PaymentTally` vs `PaymentTallyErrors` en el comentario), `R3-UnclearedTimeout` (timer del banner sin limpiar), `R4-ImportFailureUnsurfaced` (fila inválida de import sin cobertura a nivel merge). Candidato revisado: `b20f63fc`→`de0aa66f`.
- 2026-09-18: **advisories del review RESUELTOS/ACEPTADOS** — `53fa4f10` (7 archivos, +100/−22): **C** gate de módulo 16 unificado en `hasMultiPaymentsModuleAvailable` (robusta ante `storeModuleIds` ausente) y helper local eliminado; **D** el selector alinea al padre (`onChange`) cuando la preferida no está entre las opciones (etiqueta vs total); **E** labels CUP/USD vía `currencyLabel`; **F** comentario `PaymentTallyErrors.*`; **G** timer del banner limpiado al desmontar; **H** test de merge con fila inválida → `ChannelRatesUnexpectedError` y nada insertado. **Aceptados sin cambio (con razón)**: `R1/R3/R4-PaymentTallyThrow` (el throw es el contrato documentado/testeado; sin call sites aún) y `R1-ChannelRateIdScheme` (teórico pre-release: no hay filas con el esquema viejo persistidas). Evidencia: app 3788/3788; domain 164/164; typecheck 5/5; lint 4/4.
- 2026-09-18: **review del commit de advisories (`53fa4f10`) CERRADO — APPROVED + acknowledged (`authority: burned`)** — lineage `review-24925483e75e237b`, 4R, candidato `de0aa66f`→`53fa4f10` (7 archivos, 122 líneas; `high` por `authorization-service.ts`). **7 advisories nuevos, NO bloqueantes**: WARNING — `R2-1` (el ref del timer guarda SOLO el último timeout: un segundo guardado antes de los 3s deja vivo el anterior, que dispara tras el desmontaje y contradice el comentario), `R3-1`/`R4-001` (el efecto de alineación depende de `onChange`: con un callback inestable del padre podría repetir la llamada — con el padre real `setState` es estable); SUGGESTION — `R2-2` (ternario duplicado con el early return), `R2-3`/`R3-3` (el test de clearTimeout no ata el handle al timer del banner), `R3-2` (ramas defensivas null/no-array sin test).
- 2026-09-18: **7 advisories del review del fix RESUELTOS** — `66d3f4e3` (4 archivos, +55/−9): `R2-1` limpiar el timer anterior antes de reprogramar el banner; `R3-1`/`R4-001` acotar el efecto de alineación con `lastEmitted`; `R2-2` quitar el ternario redundante (con guard `Array.isArray(items)` para un slice malformado); `R2-3`/`R3-3` test que ata el `clearTimeout` al handle real del banner; `R3-2` tests de las ramas defensivas. Su propio review → **APPROVED + acknowledged** (lineage `review-4e1041ab194b0361`; app 3792/3792). **7 advisories nuevos (micro, no bloqueantes)**: `R2-currency-fallback-ref`/`R4-2` (el reset del ref se llavea en convergencia en vez de divergencia: si `value` cambia entre dos monedas ausentes, o si el módulo cae y vuelve, se suprime el aviso), `R3-second-submit-guard-unproved` (el guard de doble submit sin test), `R4-1` (el guard `Array.isArray` no cubre ELEMENTOS malformados), `R2-banner-magic-3000`/`R3-banner-timer-selection-fragile` (3000 mágico + selección del handle por delay), `R3-cart-currency-ref-coverage` (cobertura de los nuevos caminos).
- **Recomendación**: congelar la persecución de advisories micro (el código está APROBADO y cada ronda de fix genera otra ronda de advisories menores); agruparlos en T7/T12 o en un pase de limpieza único.
- 2026-09-18: **T7 (componente) listo** — `750faf44` (`multi-payment-list.tsx` + tests + 12 claves i18n; 3 archivos, +651): lista de pagos controlada (método+moneda+monto → convertido a la moneda de la orden), resumen pagado/restante/vuelto, bloqueo de cierre si no cubre, filtro de filas ≤0, error tipado si no hay tasa; gated por módulo 16; **NO toca cart-shell ni el submit** (falta montarlo y definir el límite T7/T9 de persistencia). RDD assess: **medium** → diferido. Evidencia: 16/16 dirigidos; typecheck 5/5; lint 4/4.
- 2026-09-18: **Ventas del día — gear de editar/eliminar** (`c3d8f752`; workstream aparte, misma rama): `order-list.tsx` reestructurado (fila flex: info+precio → **gear** `order-actions-toggle-<id>` → chevron `order-panel-chevron-<id>`, sin anidar interactivos); Editar/Eliminar movidos del panel al `ActionMenu` (intents `edit`/`delete` con ícono+color; testids preservados); `order-item-list.tsx` queda solo con la tabla; tests unitarios actualizados + cobertura nueva; **E2E `edit-delete-order.spec.ts` editado con autorización explícita del owner** (click al gear antes de la opción). Evidencia: vitest 3803/3803; typecheck 5/5; lint 4/4. **PENDIENTE: validar el E2E** — el server de `:3333` está tomado por otro checkout (`D:\Projects\AutoBusinessPro\Store\store-mgmt`, PID 31288) y Playwright lo reusó (`reuseExistingServer: true`), sirviendo otra build; requiere decisión del owner.
- 2026-09-18: **Ventas del día — E2E validado (parcial)**: el ambiente se corrigió (backend E2E en `:5019` con perfil `http-e2e` + borrar el cache de Vite del app) → el flujo ya corre. **`eliminar una orden` PASÓ verde** (valida el camino gear→Eliminar→confirmar→remover). **`editar una orden` queda rojo por un label VIEJO del spec**: espera `'Tarjeta'`, pero la app renombró el display de `PaymentType.Tarjeta` a **"Transferencia"** (plan `payment-methods-percent-tax`, 2026-09-17; `app/sales/components/edit-order-modal.tsx:26`) → **E2E rojo desde entonces, ajeno a este cambio**. El trace confirma que el modal ABRE bien vía el gear (nuestro cambio funciona). Propuesta: 1 línea (`'Tarjeta'`→`'Transferencia'`) — **aplicada con autorización** (`ca017d50`) y **E2E 2/2 VERDE** (40.4 s) contra app+backend reales.
- Nota: este doc se mantuvo **modificado sin commitear** a propósito durante el desarrollo; se commitea en el cierre (T12) con la evidencia final.
- 2026-09-19: **Unidad correctiva de decisiones A + 8 aplicada** (los dos flags abiertos de T7/T9 quedan resueltos en el working tree, pendiente de commit): **(A) unidades de `OrderPayment`** — la frontera documental/efectiva pasa a **UNIDADES de la moneda de la orden** (misma unidad que `Order.total`, espejo backend `decimal(18,2)`): `multi-payment-settlement.ts` convierte internamente en centavos (el tally del dominio exige enteros) pero persiste `amount`/`amountInOrderCurrency` en unidades (`round2`); `order-payment.ts` documenta el contrato de unidades. **(8) percent/tax con multi-pago** — con una lista de `payments` no vacía el total persistido es la **suma de líneas sin pricing** y `percent`/`tax` quedan en 0 (`order-offline-service.createOrder`); `cart-shell` alinea el total mostrado/validado a la misma suma sin pricing mientras el multi-pago está activo, y la ruta legacy (sin `payments`) conserva el pricing byte-idéntico. Evidencia: vitest dirigido 185/185; suite app **3816/3816**; domain **164/164**; `turbo typecheck` 5/5; `turbo lint` 4/4. Commit `a5bedde6` (6 archivos, +141/−27).
- 2026-09-19: **Review RDD del slice acumulado (T7 componente + gear "Ventas del día" + T9 + T7 wiring + unidad correctiva) CERRADO — APPROVED + acknowledged (`authority: burned`)** — lineage `review-2f422878aa499f16`, base `66d3f4e3` (tree `c41a86b4…`), `--committed-only`, 16 paths / 1671 líneas, tier **medium**, 1 lente (`review-reliability`), correction budget 200 (**no se abrió corrección**). Consentimiento del owner: **Revisar este cambio**. **3 advisories, todos NO bloqueantes** (trabajo posterior, NUNCA re-correr el review de este candidato): `R3-SETTLE-FILTER` (WARNING — `settleMultiPayments` empuja al tally todo monto convertido sin el filtro `>0` que sí aplica la lista; una fila positiva que redondea a 0 centavos haría divergir los dos guards que el header dice inseparables), `R3-SETTLE-UNPROVED` (WARNING — las ramas de error de conversión y de procedencia cross-currency de `settleMultiPayments` no tienen test; el bloqueo del submit por fila no convertible queda sin aseverar), `R3-RATE-MOMENT` (SUGGESTION — la lista y `cart-shell` capturan `new Date()` por separado, así que el momento de tasa congelado podría diferir). **Nuevo boundary RDD: `a5bedde6`.**
- 2026-09-19: **Unidad correctiva commiteada** — `a5bedde6` (`fix(multipayments): persist payment amounts in order-currency units and drop pricing with multi-pago`): decisiones A + 8 (ver arriba).
- 2026-09-19: **T8 CERRADA y revisada (approved)** — commit `21691443` (`feat(cart): allow mixed-currency carts with module 16 by converting lines to the sale currency`; 10 archivos). **Review RDD: APPROVED + acknowledged (`authority: burned`)** — lineage `review-84e4769489292baa`, base `a5bedde6` (tree `bcf279fe…`), `--committed-only`, 10 paths / **645 líneas**, tier medium, lente `review-reliability`, correction budget 200 (sin corrección). Consentimiento: **Revisar este cambio**. **4 advisories, todos NO bloqueantes**: `R3-001` (WARNING — en el camino de fallo de conversión la línea cae al precio nativo (`round2((item.price ?? product.price) * qty)`) pero se pinta con `money()`/`formatWholesaleLine(..., saleCurrency)`, así que una línea CUP se ve como si fuera USD y contradice el total del header, que excluye esa línea), `R3-002` (WARNING — el bloqueo duro por error de conversión de línea **también en ventas a crédito** es comportamiento nuevo sin test), `R3-003` (SUGGESTION — `new Date()` dentro del `useMemo` sin dependencia de tiempo congela el instante de tasa), `R3-004` (SUGGESTION — el wiring del guard con módulo 16 en las 3 rutas no está ejercido; solo se probó el camino módulo-off). **Nuevo boundary RDD: `21691443`.**

## Próximo paso

- Review slice 2: **CERRADO (APPROVED + acknowledged, authority burned)** — candidato `5f08cdee`→`b20f63fc`; nuevo boundary para assessments: `b20f63fc`.
- Reporte upstream del defecto de gentle-ai: **diferido** (no hay `gh` en PATH); el defecto del STATUS post-corrección quedó de facto esquivado por el recovery.
- **Review del fix 2 (`66d3f4e3`): CERRADO (APPROVED + acknowledged, authority burned)** — lineage `review-4e1041ab194b0361`, 4R. **Boundary RDD vigente: `66d3f4e3`.**
- 2026-09-18: **T7 (wiring) + T9 (persistencia) CERRADAS** — `7cad3aef` (T9: `Order.payments?` + `createOrder(..., payments?)` trailing opcional + revival de fechas anidadas; 3 archivos, +186/−2) y `1b7c8d9d` (T7 wiring: `payments` en el cart store, `MultiPaymentList` montado con módulo 16 reemplazando el bloque legacy, guard de cobertura en Registrar, submit con `payments[]`, `multi-payment-settlement.ts`; 5 archivos, +391/−75). Evidencia: app 3814/3814; domain 164/164; typecheck 5/5; lint 4/4. **Flags abiertos (decisión del owner)**: (1) **unidades de `OrderPayment`** — el modelo dice "integer cents" pero la orden persiste `total` en UNIDADES y el espejo backend es `decimal(18,2)` ⇒ propuesto **UNIDADES** + unidad correctiva (modelo+settlement+tests); (2) **decisión 8** (percent/tax no aplican con multi-pago; hoy el total pasa por el pricing); (3) tensión de moneda (lista con `cartCurrency()` vs total en la preferencia T6) → la reconcilia **T8**.
- **Ventas del día**: cambio hecho (`c3d8f752`, con E2E autorizado). E2E: **2/2 VERDE** (`editar` + `eliminar`, 40.4 s; label corregido con autorización en `ca017d50`). Backend E2E de la validación: `:5019` (perfil `http-e2e`, PID 24192, sigue arriba).
- **Boundary RDD vigente: `21691443`** (T8 revisado y aprobado; authority burned). Slices ya revisados: `66d3f4e3`→`a5bedde6` y `a5bedde6`→`21691443`.
- 2026-09-19: **T14 CERRADA y revisada (approved)** — ver arriba. **Nuevo boundary RDD: `2dac7dbb`.** **7 advisories NO bloqueantes** del review `review-30a374906c71497f` (nunca re-correr este candidato): `R1-1` (WARNING — el path de fallo sigue concatenando `ex.Message` al error 500; preexistente, carried forward), `R2-1` (WARNING — el literal `3` en `StorePlan.Create(...,3,...)` de los fixtures no está nombrado y se duplica en 3 archivos de test), `R2-2` (WARNING — el comentario del guard de "no vacío" en el E2E habla del set pagado, pero ya no se compara contra lo otorgado), `R2-3` (SUGGESTION — el sentinel `NotContain(16)` usa un id numérico crudo), `R3-PlanModulesNullGuard` (WARNING — `defaultPlan.StorePlanModules.Select(...)` fuera del try/catch: un plan no-null con navegación null daría NRE en vez de `Register.PlanLoadFailed`), `R3-VipExclusionVacuous` (SUGGESTION — el `NotContain(16)` pasa vacío si el id 16 se renumera/elimina), `R4-PlanOutage` (WARNING — la registración queda acoplada a que exista el plan Superior activo; sin fallback degradado).
- 2026-09-19: **T10 CERRADA y revisada (approved)** — boundary intermedio `a7f656ba` (ver arriba).
- **Boundary RDD vigente: `91144be3`** (T11 revisado y aprobado; authority burned). Slices revisados: `66d3f4e3`→`a5bedde6`, `a5bedde6`→`21691443`, `21691443`→`2dac7dbb`, `2dac7dbb`→`a7f656ba`, `a7f656ba`→`91144be3`.
- **Próxima tarea: T12 — Cierre** (verificación completa vitest/typecheck/lint + `dotnet build`/tests + E2E nuevos; actualizar este doc con evidencia; commit final del feature doc y del contrato `docs/contracts/pagos-y-canales-de-pago.md` si el owner lo quiere).
- 2026-09-19: **T10 CERRADA** (ver arriba): primera corrida roja por la premisa falsa del spec (corregida en T14: el alta ya no otorga el catálogo entero) y luego por el cifrado DEK del login real (corregida en el spec con `dropDekAndCiphertext`). **2/2 VERDE**, review APPROVED + acknowledged.
- **Advisories acumulados (NO bloqueantes, trabajo posterior)**: del slice `a5bedde6` — `R3-SETTLE-FILTER`, `R3-SETTLE-UNPROVED`, `R3-RATE-MOMENT`; del T8 (`review-84e4769489292baa`) — `R3-001` (WARNING, línea no convertible se pinta como moneda de venta y contradice el total), `R3-002` (WARNING, bloqueo duro en crédito sin test), `R3-003` (SUGGESTION, `new Date()` sin dependencia de tiempo), `R3-004` (SUGGESTION, wiring del guard con módulo 16 sin test). Agrupar en un pase de limpieza; **NUNCA re-correr el review de los candidatos `a5bedde6`/`21691443`.**
- Nota E2E: el frontend E2E requiere app+backend reales; el `:3333` puede estar tomado por otro checkout (`reuseExistingServer`) y el backend E2E usa `:5019` (perfil `http-e2e`). Verificar el ambiente antes de correr T10.
