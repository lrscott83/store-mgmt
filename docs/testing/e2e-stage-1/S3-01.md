# [S3-01] Exportar el roster de aprovisionamiento

> Etapa 1 · Bloque C — Gestión de usuarios · [← plan general](README.md)

| | |
|---|---|
| **Prioridad** | ALTA |
| **Personas** | OwnerAdmin |
| **Endpoint(s)** | `GET /v1/storeusers/{storeId}/offline-roster` (`management/users/components/roster-export-panel.tsx:45`) |
| **E2E frontend (Playwright)** | **CUBIERTO** |
| **E2E backend (.NET)** | **CUBIERTO** |

## User Story

Como OwnerAdmin, quiero exportar el roster cifrado de mi tienda a un archivo, para aprovisionar un dispositivo que deba operar sin internet.

> **No confundir con sync export/import.** Aquél es archivo puro sin API; éste **sí** es una llamada HTTP cuyo resultado se serializa y se descarga (`roster-export-panel.tsx:45-58`). Alimenta `auth/routes/provision.tsx`, que es la precondición de [S1-03](S1-03.md).

## Precondiciones

- OwnerAdmin autenticado con feature `Users` (`user-list.tsx:12` — `adminFeatureLoader([EFeatures.Users])`).
- Navegador online y `selectedStoreId` no vacío.

## Flujo

1. Ir a `/management/users`.
2. Presionar "Exportar roster".
3. Ingresar la contraseña maestra y confirmar.

## Aserciones — Playwright (UI)

- [x] El botón está **habilitado** online (`roster-export-panel.tsx:33`). Verificado en `e2e/roster-export.spec.ts`. — Disabled offline verificado en vitest (`roster-export-panel.test.tsx:44`).
- [x] Confirmar con contraseña maestra vacía muestra `SYNC.ERROR_EMPTY_PASSWORD` y **no** emite petición (`roster-export-panel.tsx:38-41`). Verificado en `e2e/roster-export.spec.ts`.
- [x] La confirmación exitosa dispara exactamente un `GET /v1/storeusers/{storeId}/offline-roster` (`roster-export-panel.tsx:45`). Verificado en `e2e/roster-export.spec.ts`.
- [x] Se descarga un archivo llamado `roster-{storeId}.smcabundle` (`roster-export-panel.tsx:56`). Verificado en `e2e/roster-export.spec.ts`.
- [x] Los bytes descargados empiezan con la firma ZIP `PK\x03\x04`, no con `{`. Verificado en `e2e/roster-export.spec.ts`.
- [x] El archivo **round-trip**: `deserializeRoster(payload, master, storeId)` devuelve el bundle original. — Cubierto por vitest (`roster-serializer.test.ts`); el E2E verifica la ZIP signature como proxy.
- [x] Una contraseña maestra incorrecta produce `WrongPasswordError`, archivo corrupto produce `CorruptFileError`. — Cubierto por vitest (`roster-serializer.test.ts:27-41,88`).
- [x] El `storeId` es parte de la contraseña (cross-store aislado). — Cubierto por vitest (`roster-serializer.test.ts`).
- [x] Tras el éxito, el panel se cierra (`roster-export-panel.tsx:60`). Verificado en `e2e/roster-export.spec.ts`.
- [x] Un fallo de la petición muestra `USERS.ERROR` y mantiene el panel. — Cubierto por vitest (`roster-export-panel.test.tsx:61`).
- [x] El toggle de visibilidad de contraseña alterna `SYNC.SHOW_PASSWORD` / `SYNC.HIDE_PASSWORD`. Verificado en `e2e/roster-export.spec.ts`.

## Aserciones — .NET E2E (dato)

- [x] Un OwnerAdmin puede exportar el roster de **su propia** tienda; el de una tienda ajena es rechazado.
- [x] Un StoreUser sin permiso recibe **403**.
- [x] `ExpiresAt = IssuedAt + ttlDays`, con `ttlDays` leído de `SystemConfiguration` (`Application/Features/Management/Users/Queries/ExportOfflineRoster/ExportOfflineRosterQuery.cs:147,153`).
- [x] Sin fila de configuración, el TTL cae al **default de 35 días** (`Infrastructure/Persistence/Repositories/SystemConfigurationRepository.cs:46-49`). Semilla `"35"` en `Infrastructure/Persistence/EntityConfigurations/SystemConfigurationEntityTypeConfiguration.cs:38-39`. Ver [AUTH-INV-01](AUTH-INV-01.md) y **H-2**.
- [x] Cada usuario del bundle lleva `PaymentDueDate`, `IsInTrial` y `PaymentStatus` derivados del resumen de facturación (`ExportOfflineRosterQuery.cs:140-142`).
- [x] Una tienda `Vencido` exporta **solo** módulos `priceIncluded`.

## Estado de cobertura

**E2E backend (.NET) — CUBIERTO**
`backend/src/SMCA.WebApi.E2ETests/Users/ExportOfflineRosterTests.cs:34,85,109,128,153,176,193,278,310,348,379,443,477,511,559` — incluye `:443` TTL configurado = 7 aplicado, y `:477` sin fila ⇒ default **35**. También `Application.Tests/Management/Users/Queries/ExportOfflineRoster/ExportOfflineRosterQueryHandlerTests.cs:39-274`.

**E2E frontend (Playwright) — CUBIERTO**
`e2e/roster-export.spec.ts` (3 tests): descarga ZIP con nombre correcto + signature PK\x03\x04 + panel cierra, contraseña vacía sin petición, botón habilitado online. 7 de 12 aserciones verificadas en Playwright; 5 restantes cubiertas por vitest (round-trip, WrongPasswordError, cross-store, error display, offline button).

**Nota**: `vitest` cubre habilitación/deshabilitación y la serialización en `management/users/components/__tests__/roster-export-panel.test.tsx:44,50,56,61,80`. No es E2E.
