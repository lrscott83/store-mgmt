# Tasks: Change Password Endpoint Fixes

**Change**: `change-password-endpoint-fixes` — `POST /api/v1/users/change-password` → `POST /api/v1/users/change-password/{id}` (plan #22)
**Sequence**: RED→GREEN; mirror archived `2026-08-01-user-roles-endpoint-fixes`. **NO GIT COMMITS** — dirty tree untouched; gates are build + E2E (Postgres `smca_test` up).

## Phase 1: RED — E2E rewrite (write failing tests)

- [ ] 1.1 `backend/src/SMCA.WebApi.E2ETests/Users/UsersChangePasswordTests.cs` — rewrite both existing tests (:16-29, :31-48 — old body-UserId contract) to the `{id}` route, 8 tests per design D6:
  - RED: `Change_own_password_returns_200_and_relogin` (SuperAdmin self → 200 `Succeeded true`; re-login NEW → 200; login OLD → **401** — real-401 assert pattern `AuthLoginTests.cs:31-43`: status + `Succeeded false` + `ActionCode 401`); `Change_password_with_wrong_old_password_returns_400` (self, wrong old → **400** + envelope failed); `Change_password_cross_tenant_owner_admin_returns_404` (OwnerAdmin actor → custom-tenant victim → **404**; today 200 IDOR); `Change_password_super_admin_cross_tenant_returns_200` (→ 200)
  - GREEN guards: `Change_password_with_weak_new_password_returns_400` (`"abc123"` 7ch, `"alllowercase123"`); `Change_password_with_nonexistent_id_returns_400` (random Guid); `Change_password_same_tenant_owner_admin_returns_200` (target ≠ actor, `Succeeded true`); `Change_password_as_store_user_without_permission_returns_403` (status-only — `ForbidResult` empty body)
  - Cross-tenant victims (5/8): inline `Tenant.Create` + `User.Create(..., tenantId)` + `UserRole.Create` (`SeedUserWithRoleAsync:108-118` pattern); cleanup `CleanupTenantCascadeAsync` + `AuthzSeed.CleanupStoreGraphAsync`; **apply-time check: `User`→`Tenant` FK?**
  - Verify: filtered run → tests 1/2/5/8 FAIL (today 200s), 403 test FAILS (old body contract)
- [ ] 1.2 Do NOT write handler/validator unit tests — repo convention: UserManagement handlers covered by E2E only (zero handler unit tests exist)

## Phase 2: GREEN — Backend (this order)

- [ ] 2.1 i18n (D5): add `PasswordMinLength` + `PasswordRequiresUppercase` `<data>` blocks (exact D5 XML, `{0}`/`{1}` indexed) to `backend/src/Resources/Localization/I18n.resx` (before `</root>` :258, Spanish) AND `I18n.en.resx` (before `</root>` :522, English). Designer.cs untouched; string-indexed `_localizer` usage. Verify: keys grep-able in both; build
- [ ] 2.2 Validator (D4): `.../UpdateUserPasswordCommandValidator.cs` — `UserExists` body (:40) → `_userRepository.ExistsAsync(userId, cancellationToken)` (single query, no `GetByIdAsync`); rename param `tenantId` → `userId` (:38); NewPassword adds `.MinimumLength(8)` + `password.Any(char.IsUpper)` with the 2 new keys (mirror `RegisterCommandValidator.cs:22-26`); keep NotNull/NotEmpty (:28-34). Verify: build; weak-password E2E → 400
- [ ] 2.3 Handler (D3, BCrypt-corrected): `.../UpdateUserPasswordCommand.cs` — `User? user` + null-guard → `Failure(UserErrors.NotFound, 404)` (:48); self branch (:49) → `VerifyPassword(OldPassword, user.Password)`, fail → 400 (:51-52 — zero `HashPassword` vs OldPassword); admin gate 400→404 (:55-56); tenant-scope `!IsSuperAdmin && user.TenantId != _httpContextService.TenantId.ToGuid()` → 404 (SuperAdmin bypass; `ToGuid()` null→Empty fail-closed); keep `HashPassword(NewPassword)` + `UpdateAsync` + `SaveChangesAsync > 0` (:58-60). NO `StartsWith('$')` branch — upgrade-by-change (every write is fresh BCrypt). Verify: build; E2E 1/2/5/8 GREEN
- [ ] 2.4 Controller (D1/D2): `backend/src/SMCA.WebApi/Controllers/v1/UsersController.cs:140-146` — `[HttpPost("change-password/{id}")]` + `[FromRoute] Guid id` + `[FromBody] UpdateUserPasswordCommand command` + `command.UserId = id`; XML comment + `[ProducesResponseType]` 200 (`ResponseResult<bool>`) /400/401/403/404; ActionCode switch mirroring `AuthController.cs:30-41` + `404 => NotFound(result)` (REAL statuses, no 200+envelope). `[HasPermission(ProfileAdmin)]` retained. **Route change kills old body-UserId contract — grep verified zero sibling tests hit it (only `UsersChangePasswordTests.cs:24,39`, both rewritten in 1.1)**. Verify: build; Swagger shows 5 statuses

## Phase 3: GREEN — Docs + verify

- [ ] 3.1 `docs/plans/endpoints-e2e-coverage.md` — row #22 (:58) → `| 22 | CRITICAL | POST /api/v1/users/change-password | UsersController.ChangePasswordAsync | ✅ Done | ✅ Archived | change-password-endpoint-fixes |` (statuses finalized at apply/archive — D7); detail section (:306-313) mirrors rows 20-21 (:288-304): Purpose / Controller / Authorization `[HasPermission(ProfileAdmin)]` / E2E Tests (8) / Coverage ✅ Full / Review ✅ Done (fix summary)
- [ ] 3.2 NEW `docs/plans/2026-08-02-change-password-contract-frontend.md` (structure: `2026-07-30-register-endpoint-fixes-frontend.md`) — BEFORE/AFTER contract table (route `{id}` + password-only body; real 400/401/403/404); consumers Angular `user.service.ts:65-66` + React `profile-http-service.ts:28-37` (admin reset REMOVED — self-service only); frontend tasks: React `change-password.tsx:24-31` must NOT `logout()` on 4xx — show `PROFILE.UPDATE_ERROR`; verify `{id}` URLs; update `profile-http-service.test.ts:82`; verification criteria (PF-CPW1-4)
- [ ] 3.3 Verify: `dotnet build` solution → 0 errors; `dotnet test backend/src/SMCA.WebApi.E2ETests --filter "FullyQualifiedName~UsersChangePasswordTests"` → 8 GREEN; regression `--filter "FullyQualifiedName~UsersChangePasswordTests|UsersListTests|UsersUpdateTests"` → all GREEN
